# Plan

1. Extend the database with stable identifiers, automation settings/evidence,
   text-reference evidence, conversion receipts, and lifecycle RPCs.
2. Add Worker routes for conversion, cycle start, calendar exports, and
   reference hydration.
3. Add UI controls to action structure, workflow settings, project details,
   and cycles.
4. Add pgTAP, Worker, and React coverage; run the full suites.
5. apply the forward migration, deploy the Worker/UI, and verify production.
